import styled from "styled-components";
import PinkButton from "../PinkButton/PinkButton";

const AuthButton = styled(PinkButton)`
  font-size: 18px;
  padding: 4px 23px;
  font-weight: 700;
`;
export default AuthButton;
